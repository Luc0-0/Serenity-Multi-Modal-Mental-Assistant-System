# Serenity

React 19 + Vite + CSS Modules + Framer Motion | FastAPI + Supabase | Vercel + Railway

## Skills (MANDATORY)

- **ANY frontend work** (create/edit/refactor/style) → `frontend-design:frontend-design`
- **New feature** → `superpowers:brainstorming` first
- **Bug/unexpected behavior** → `superpowers:systematic-debugging`; escalate to `sentry:sentry-workflow` for chaotic/prod errors
- **3+ files touched** → `superpowers:writing-plans`
- **Logic-heavy code** → `superpowers:test-driven-development`
- **Before claiming done** → `superpowers:verification-before-completion`

## Component Libraries (check before building custom)

1. Magic UI → `mcp__magicuidesign__searchRegistryItems`
2. Aceternity UI → `mcp__aceternityui__search_components`
3. Context7 → `resolve-library-id` → `query-docs` for latest API docs

## Standards

- Apple-level polish, no generic UI. Glass, gradients, depth, micro-interactions
- CSS Modules, named exports, Framer Motion for animation
- Voice check-in uses `sendChatMessage`, NOT `voiceChat` endpoint
